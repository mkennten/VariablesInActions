name: Secret
on: 
  workflow_dispatch

jobs:
  ShowContexts:
    runs-on: ubuntu-latest
    
    steps:
      - name: Show simple secret
        run: echo '${{ secret.SIMPLE_SECRET }}'
      - name: Some JSON secret
        run: echo '${{ secret.JSON_SECRET }}'

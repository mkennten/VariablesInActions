name: Tests

on:
  workflow_dispatch

env:
  VARIABLE1 : 'value1'
  
jobs:
  job1:
    runs-on: ubuntu-latest

    steps:
      - name: Print variable
        run: echo ${{ github.workspace }}
      - name: ls
        run: ls ${{ github.workspace }}

